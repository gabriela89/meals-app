class Category {
    constructor(id, title, color, imageUri) {
        this.id=id;
        this.title=title;
        this.color=color;
        this.imageUri = imageUri;
    }


}

export default Category;
